# MaiMotionDemo 使用手冊

本手冊描述 v0.4.3 的預設參數。

## 參數

「參數」預設顯示四個常用控制，可用滑桿或直接輸入小數：

| 控制 | 範圍／預設 | 調高後的行為 |
|---|---|---|
| 左右分工傾向 | 0–100／60 | 更偏好留在本側；設為 0 完全取消跨區成本，仍會比較移動與負荷 |
| 快速移動容忍 | 1–200 半徑/秒／6.5 | 超過此速度才增加高速負擔，依移動距離累計、極短促的大移動最重，優先於左右分工；預設約為 137 BPM 8 分音符跨兩個鍵的速度。不是速度上限，低速移動距離也會計分。舊版保存的預設 90 會自動換成 6.5 |
| 同點連打容忍 | 0–100／60 | 更接受同手高速重複敲同一位置；前兩顆不收同點連打成本 |
| Slide 換手意願 | 0–100／40 | 普通交接的附加成本較低；仍需符合接觸與佔用限制 |

第一次啟動預設選「人類動作 V3」；已保存過的評分方式會保留，可在「參數」切換。V3、分工優先 V2、舊版比較 V1 的數值各自保存，沒有精確換算。V2 預設仍為 80／30／60／40，V1 顯示原本六項權重。時間、搜尋寬度、手掌半徑等在「進階設定」調整。設定變更後需重新生成，播放倍率只改播放時鐘。

V3 方案依三群分數的總和排序：「動作效率」（移動距離、高速移動、Slide 趕接）、「姿態與分工」（跨區、雙手交叉、Slide 換手）、「短期負荷」（同點高速連打、反覆折返、單手集中負荷）。跨區主要在深入另一側時收費，不是每顆對側音符重收；忙碌手繼續動作的負荷會增加，閒置本身沒有懲罰。分項可展開查看及複製，便於比較不同打法。

例如 `(120){4}1,1,E` 通常同手；`(240){24}1,1,1,1,E` 開始有雙手誘因；`(240){24}1,2,1,2,1,2,E` 可由兩手各守一點。這些由通用成本產生，不是固定鍵位特判，其他音符、參數與搜尋寬度都可能影響結果。

V2 保留原本的分工／動作負擔加權比較，距離只用於幾乎同分的方案。三版共用接觸判定與 Rust 幾何。必要 Slide 追蹤與額外趕接壓縮分開計分；雙手交叉是平面姿態近似，不是手臂碰撞模擬。

這些預設仍是 Demo 假設，未經玩家資料校準。分數不是成功率或人體能力；Beam Search 不保證全域最佳。

## 1. 這個 Demo 能做什麼

把 simai 譜面本文轉成左右手的觸碰與滑行動畫。每個候選方案都包含成本、手的移動軌跡與 Slide 中途交接。你可以慢放、拖曳時間、比較不同方案，再調整模型偏好。

## 2. 啟動

### 已建置的執行檔

從 [GitHub Release](https://github.com/silver0416/MaiMotionDemo/releases/latest) 下載 `MaiMotionDemo-vX.Y.Z-windows-x64.exe` 後直接開啟；從原始碼建置的檔案位於 `src-tauri/target/release/mai-motion-demo.exe`。正式版本內嵌前端，不需要 Node.js 或 Vite 伺服器；Windows 仍需 WebView2 Runtime。

### 更新

每次開啟都會自動檢查 GitHub 上有沒有新版。有新版時右下角會提示，按「下載更新」就把新版下載到目前程式所在的資料夾（這個資料夾不能寫入時改放到「下載」資料夾），通知會顯示已下載的大小與進度條，下載後會核對檔案的 SHA-256。完成後按「重新啟動」，程式會開啟新版並關閉舊版；新版開啟後會詢問要不要刪除舊版的執行檔，不刪也不影響使用。紀錄、設定與下載的影片都存在本機資料夾，更新後全部保留。也可以在「設定」的「軟體更新」手動檢查、下載與重新啟動。v0.4.0 以前的版本沒有這個功能，請先手動下載一次。

### 從原始碼執行

準備 Rust、Microsoft C++ Build Tools、WebView2 及 Node.js。開發與建置已在 Windows、Rust 1.90、Node.js 24 驗證。

在專案根目錄的 PowerShell 執行：

```powershell
npm.cmd ci
npm.cmd run tauri dev
```

第一次建置會下載與編譯 Rust 依賴，所需時間較久。

建立正式執行檔：

```powershell
npm.cmd run tauri build -- --no-bundle
```

單獨執行 `npm.cmd run dev` 只會開啟介面預覽，沒有 Rust 核心，不能解析輸入。

## 3. 第一次展示

1. 按左側「新增」，貼入 `(120){4}1,8,2,7,3,6,4,5,E`，按「生成並新增」呼叫 Rust。
2. 按「播放」。粉紅圓形 L 代表左手，藍色圓形 R 代表右手；兩手軌跡都是實線，以顏色與 L/R 文字區分。
3. 試用 0.5× 慢放，或拖曳時間軸觀察某個瞬間。負時間代表首音前的預備動作。
4. 再新增 `(120){4}1-5[4:1]/5-1[4:1],E`，觀察兩條 Slide 碰頭時兩手交換目的地。
5. 切到「方案」，查看候選成本、兩手的起始觸碰／滑軌參與，以及交接區間。「定位」可跳到交接位置。

左側紀錄以名稱為主標、新增時間為副標；新增時可填名稱，留空則使用譜面開頭（`&title` 或第一行）。紀錄、分析結果與「顯示」「參數」設定都存在本機資料庫：同一份譜面在相同設定下分析過，再次點選會直接載入，不重新分析；改了參數才會重跑。兩手互換不同於普通交接，沒有重疊時間，也不收普通交接費用。

### 從 Majdata 搜尋匯入

按左側「搜尋」會開啟搜尋視窗，上方有「simai Wiki」「Majdata」兩個分頁，切換分頁時各自的輸入、篩選與結果都會保留。「Majdata」分頁輸入歌曲名稱、作者、譜師或紀錄名稱後按 Enter 或搜尋按鈕，會同時列出符合的本機紀錄與 Majdata 結果；上方「難度」可只看含某個難度（EAS～ORG）的譜面。按 Majdata 某筆的「匯入」後，程式會下載該份 `maidata.txt`，先交給 Rust 核心解析與分析，成功或可顯示譜面時才加入左側紀錄；有多個難度時分析編號最大的那一個。

已經匯入過的譜面（Majdata song id 相同）標示「已在本機」，按「開啟」直接跳到本機紀錄，不會重新下載。下載後若發現本機已有原文逐字相同的譜面，也會直接開啟既有紀錄；同名但作者或內容不同的譜面仍視為不同譜面。

這個入口只下載譜面文字，不會下載或播放音樂、影片與封面。搜尋與下載需要連線 `majdata.net`；解析、左右手搜尋與動畫資料仍在本機完成，也不需要 Majdata 帳號。線上譜面由各上傳者提供，內容、可用性與授權由來源端決定；請依來源規範使用。

### 從 simai Wiki 搜尋匯入

切到「simai Wiki」分頁，第一次使用時會載入譜面索引（來源為 simai Wiki），之後可按「重新整理索引」重新抓取。索引會快取在本機：離線或 Wiki 暫時不可用時會沿用快取並提示；完全沒有快取又載入失敗時才無法搜尋，可按「重試」。

輸入關鍵字後按 Enter 或搜尋按鈕，結果依歌曲列出，各難度分開顯示。已經匯入過的譜面標示「已在本機」，按「開啟」直接跳到本機紀錄，不會重新下載；下載後若發現本機已有原文逐字相同的譜面，也會直接開啟既有紀錄。按「匯入」後程式只下載該難度的譜面文字，交給同一套本機 Rust 解析與左右手分析；不會下載音樂、影片或封面。

## 4. 自行輸入 simai

貼上**譜面本文**，按「生成」。也可以直接貼整份 `maidata.txt`，或別人分享的真人手順標註檔（見第 8 節）：程式會取難度編號最大的 `&inote_n`，並在診斷區說明選了哪一個；`&first=` 只會提示，不會自動套用。整份檔案及含 `<HS*倍率>` 的 Majdata 譜面可省略結尾 `E`，診斷區會提示；一般 simai 本文仍需 `E`。

基本 Tap：

```text
(120){4}1,2,3,4,5,6,7,8,E
```

Hold 佔住左側，另一手接後續音符：

```text
(120){4}8h[4:4]/1,2,3,4,E
```

Slide：

```text
(120){4}1-5[4:3],8,7,6,E
```

慢速圓弧與顯式等待：

```text
(120){4}8>4[1##3],,,,,,,8,E
```

### 支援語法

| 類型 | 寫法 |
| --- | --- |
| BPM 與分割 | `(120)`、`{4}`；可在任何位置變更 |
| 固定秒數步長 | `{#0.25}` |
| 推進與結束 | 逗號推進一格；最後用 `,E` |
| 註解 | `\|\|` 之後到行尾 |
| Majdata 顯示速度 | `<HS*1.5>`；Demo 會讀取並提示，不套用落下顯示速度 |
| Tap | `1`–`8` |
| 同時音 | `/`，或直接連寫 `18`；疑似 EACH 用反引號 |
| 音符修飾 | Break `b`、EX `x`、星形 `$`、旋轉星 `$$` |
| Hold | `1h[4:1]`、`1h[#1.5]`、`1h[120#4:1]`；省略長度的 `1h` 視為長度 0 |
| Touch | `A1`–`A8`、`B1`–`B8`、`C`（`C1` `C2`）、`D1`–`D8`、`E1`–`E8`，煙火 `f` |
| Touch Hold | `Ch[4:1]`、`C1h[#1]f`；省略長度的 `Ch` 視為長度 0 |
| Slide 形狀 | `-` `^` `<` `>` `v` `V` `p` `q` `pp` `qq` `s` `z` `w` |
| Slide 長度 | `[4:1]`、`[#1.5]`、`[160#8:1]`、`[等待秒##移動規格]` |
| Slide 組合 | 連續 `1-3-5[4:1]`、接續 `1-3[4:1]-5[4:1]`、同頭 `1-3[4:1]*-7[4:1]` |
| 無起點 Slide | `1?-5[4:1]`、`1!-5[4:1]` |
| Slide 修飾 | 長度後面加 `b` 或 `x` |
| Majdata Break Slide | 長度前也可加 `b`，如 `2>6qq4b[4:1]` |

`[n:m]` 表示 m 個 n 分音符。一般 Slide 的起點觸碰後先等待一拍，再開始指定長度的移動；等待時不會把起點手整段鎖住。`V` 的轉折鍵必須是起點左右各兩格；`s` `z` `w` 只能連到正對面的鍵位；`-` 的終點需相隔 2–6 格；`^` 只接受短於半圈的圓弧。幾何不合法的起終點會報錯並標出位置。

貼整份 `maidata.txt` 或使用 Majdata HS 指令時，反引號分開的音符使用 Majdata 的 128 分音間隔；一般 simai 本文保留原有位移。

整首譜面可以直接貼進來。上限是 10000 個音符、3600 秒、單一長音 120 秒、原文 4 MB，這些是防呆用的安全值，不是一般譜面會碰到的限制；真正的界線是搜尋的計算預算（約 20 秒），超出會回報 `search_limit`，可縮短片段或降低搜尋寬度。語法定義見 [simai 說明](https://w.atwiki.jp/simai/pages/1002.html)。

### 形狀近似

`p` `q` `pp` `qq` 畫成沿指定方向捲進內圈繞行再捲出，`s` `z` 畫成通過中心的平滑 S／Z，`w` 以中央那條當手的移動路徑、另外畫出兩條側線。Touch 的 A／D 在外圈、B／E 在內圈，D 與 E 相對旋轉半格。這些位置與曲線是本 Demo 的可辨識近似，不是實機軌道座標，會影響成本高低。

## 5. 面板與控制

- **編輯**：輸入、範例、生成、錯誤位置及支援語法。重新生成不會改寫你輸入的文字。
- **方案**：比較候選成本。起始觸碰統計包含 Tap、Hold 與 Slide 起點；滑軌參與另列，一條換手的軌道可能同時計入兩手。
- **音符**：查看選定音符的時間、分配與滑行區段。
- **參數**：選擇 V3／V2／V1，調整該版打法偏好，以及搜尋寬度、交接開關、相鄰連擊滑移距離與 Slide 最晚接上時間。變更後需重新生成。
- **標註**：逐顆記錄真人的左右手、Slide 換手、信心與備註，可複製分享、匯入合併、和模型比對，也可以開影片視窗對照真人手元影片，見第 8 節。
- **顯示**：軌跡、音符、手掌覆蓋區、縮放及背景中心／半徑校準。校準只改顯示，不改 Rust 的幾何資料。「最近軌跡」預設開啟；「整段軌跡」預設關閉，長譜面開啟後盤面會佈滿細線。

在「參數」可切換「允許一隻手掌同時覆蓋多個 Touch」，或調整「手掌半徑」（0–1；0 等於關閉）。預設 0.5 的圓形覆蓋面積約為四分之一盤面。改動後須重新生成；瀏覽器預覽沒有 Rust 核心，不能重跑搜尋。

在「顯示」也可切換 Touch 落點標記：預設關閉，可改成有 Touch 時顯示或一律顯示，並可關閉標記上的區域編號。A／D 使用四邊形，B／E 使用三角形，C 使用中央八邊形；Touch Hold 的雙層輪廓顯示按住進度。`f` 煙火只在起始判定後短暫擴散，可獨立關閉；拖曳時間軸到相同秒數會看到相同效果。這些輪廓是辨識用示意，不代表實機感應區大小。

一掌覆蓋時，盤面顯示該手顏色的大圓虛線、掌心標記及被覆蓋 Touch 的圈線；時間軸上緣細條表示佔用區間。這些位置、半徑與時間都取自分析結果。在「顯示」關閉「顯示手掌覆蓋區」只隱藏畫面，不改變方案。選取被覆蓋的 Touch，在「音符」可查看這一掌的手、覆蓋落點／音符、時長、掌心和半徑。

播放倍率為 0.25×、0.5×、1×、2×，只改播放時鐘，不改分析結果。循環片段可用「設為起點／終點」設定範圍。空白鍵播放／暫停，左右方向鍵微調，Shift 加方向鍵移動一秒，Home 回到起點；在文字輸入欄中不會攔截一般輸入。

## 6. 如何理解結果

成本越低，只表示目前設定下模型越偏好。Beam Search 會剪除候選，所以不保證全域最佳，也不能把成本差換成人類打法的真實機率。

Tap 與 Slide 將每手簡化成單一接觸點；也可用一隻圓形手掌覆蓋同時出現的多個 Touch／Touch Hold。已按住的 Touch Hold 也能在後續鄰近 Touch 到來時擴展成手掌覆蓋，前提是新落點仍在同一個掌心與半徑內。全盤級的大量同時 Touch 則可用雙手連續掃屏，兩手各負責半邊，滑行路徑可以經過 C 與內圈。Hold 與 Touch Hold 期間固定佔用；Slide 沿軌跡接觸；交接時兩手會有短暫重疊覆蓋。同一時間落在同一位置的多個接觸由同一隻手一併完成（例如 Slide 起點上的 Break Tap）。

兩條對穿的 Slide 如果會在某一瞬間經過同一點，模型會讓兩手在碰頭時**互換目的地**，而不是穿過去讓手臂一直交叉 —— 這也是玩家的實際打法。互換不需要移動，所以不收交接費用；盤面上標成「兩手互換」。只是空間交叉、兩手沒真的碰到的情形不適用，那種手臂交叉本來就是正常的。

相鄰鍵位的高速連擊，模型會讓同一隻手不抬起、貼著面板滑過去，而不是雙手交替 —— 這是玩家實際的打法。判定條件有兩個：連續兩次接觸的距離在「相鄰連擊滑移距離」（預設 0.8；相鄰鍵約 0.77、隔兩鍵 1.41）以內，而且間隔短於「同手連打判定間隔」（預設 0.15 秒）。間隔夠長就有餘裕抬手，模型也會抬手 —— 滑移是來不及抬手才用的技巧。同一個鍵連打（距離 0）一律算抬手重按。盤面上這段會標成「滑移中」。

手被前一組音符佔住時，模型會參考 maimai DX 的判定規則（60 fps，一幀 1/60 秒）找空手的方法，目標仍是全部 Critical Perfect：
- 滑向下一個相鄰目標時，上一個 Tap 或 Touch 手掌只需停留一幀，不必停滿「敲擊接觸時間」。例如一手按住長條、另一手在 48 分音間隔內連續擦過好幾組 Touch。
- Hold 與 Touch Hold 結尾 12 幀（0.2 秒）不檢查按壓，需要時可提早放手；很短的 Hold 只看頭判。
- Touch 沒有 Fast 判定，正解後 9 幀（0.15 秒）內接觸仍是 Critical Perfect。只有整組無法準時完成時才會晚接；若全譜因此無解，會允許晚接重跑一次，方案說明會註明。
- 追 Slide 或按著螢幕的手在 Touch 判定區間內經過落點附近（0.28 以內）時，這個 Touch 直接算完成，不另外移動。外圈按鍵在螢幕外，按住按鍵不會碰到 A 區。

- 同一時刻、判定區相鄰的 Touch 會組成 Touch Group：實際碰到超過半數時，剩下的會連帶判定。只有手真的接不到時才這樣安排，方案會註明，音符明細標示「Group 連帶判定」。
- Slide 的尾判看的是手何時進入最後一個判定區，正解時刻與 Critical Perfect 寬度會隨形狀與 Slide 長度改變（例如 `1-5` 在移動時長的 84.8% 進入 A5）。若 Slide 結束前還有別的音符需要這隻手，手可以在進入最後判定區後就離開，不追到終點；方案會註明。

人類動作 V3 依 Slide 的判定區順序抄近（規則取自 MajdataPlay 的判定佇列，是社群重製版規則）：碰到下一個判定區就能跳過目前這區，但不能連續跳兩區，標為不可跳過的判定區一定要碰到。手走的是能完成判定的最短路線，並在最後判定區的正解時刻完成、之後就離開，不追到星星終點；星星仍照原路徑顯示。同時出發、同時結束且距離夠近的兩條 Slide，可由一隻手張開同時完成；追 Slide 的手剛好碰到某顆 Tap 的外圈 A 區時，也可以順手點掉。V1／V2 仍沿原路徑追到終點。

Slide 的起點觸碰與星星出發之間隔了一拍，譜面常在這段空檔安排別的音符。手不必在星星出發的瞬間就貼上軌道：被別的接觸卡住時可以稍晚才接上（預設最多 0.12 秒，可在「參數」調整），接上之後仍須在原定終點前走完整條路徑，走得越急速度成本越高。若名目終點同時出現下一組接觸，最後一個 checkpoint 也可提早掃到路徑終點，留下短暫回位時間；輸出的手軌跡仍連續。沒有這類衝突時模型仍依名目時間完成。Break 與 EX 只影響實機判定，不改變本模型的動作。尚未模擬手臂關節、真實手形或實機感測器判定；非同時 Touch 只有在既有 Touch Hold 持續期間且落在同一掌範圍內時才會合併。

「本模型未找到可行方案」不代表人類無法遊玩。可縮短片段、增加搜尋寬度，或檢查是否超出單手單接觸點的假設。

沒有方案、語法錯誤或分析失敗時，診斷下方有「複製除錯資訊」按鈕，會把完整譜面、錯誤行列（附前後幾行與標記）、相關音符、參數與原始診斷複製成一段文字，可直接貼給開發者。

例如 `(120){4}C/B1/E1/7,E`，中央與內圈 Touch 同時出現，另一手須按鍵；或 `(190){4}1h[4:15],,,Ch[1:3],,,,,B1,B8,B2,B7,B3,B6,B4,B5,E`，一手長按按鍵，另一手按住 C 後依序覆蓋整圈 B。v0.2.0 能輸出這些手掌方案並在播放時畫出範圍；半徑設為 0 時會回報沒有方案。預設半徑 0.5 是「手掌約占四分之一盤面」的 Demo 圓形換算，不代表真實判定。

若同一判定同時出現至少 16 個一般 Touch，例如 `A1/A2/.../E8/C` 的全感應區配置，求解器會產生兩條 `glide` 掃屏路徑。每隻手從當時可用的位置出發，以最近的下一個感應區安排連續路徑，最早在判定前 0.18 秒開始，並把各 Touch 的實際經過時間寫進 assignment。若前一動作仍佔用手，該手只能延後起跑，速度成本也會提高；到判定時仍無法開始就不採用掃屏。0.18 秒與 16 顆門檻都是 Demo 假設，不是官方判定窗。

## 7. 常見問題

**瀏覽器中生成按鈕不能用**：瀏覽器預覽沒有 Rust 核心。請開啟 Tauri 桌面程式。

**開發版空白或無法連線**：開發版需要 Vite 的 1420 port。使用 `npm.cmd run tauri dev` 同時啟動，或改用正式版執行檔。

**PowerShell 的 npm 報路徑錯誤**：改用 `npm.cmd`，避免本機 `npm.ps1` 啟動器問題。

**語法錯誤**：依畫面的行列位置修正。Demo 不會默默略過未知音符來產生不完整動畫。

**分析中／搜尋預算用完**：核心一次分析一份譜面，沒有取消按鈕；可等待工作結束，再縮短片段或降低搜尋寬度。

**Majdata 搜尋或匯入失敗**：確認網路連線後重試。譜面可能已被移除，Majdata 服務也可能暫時限流或變更公開存取規則。下載成功但語法不相容時，搜尋視窗會保留診斷，且不會把失敗內容加入紀錄。

**沒有音樂**：本版只有譜面與動作展示，未加入音源播放或遊戲打分。


### Slide 與 WiFi 的雙手分工

- 人類動作 V3 優先讓同一隻手滑完同一條 Slide；後面還有其他接觸任務時才考慮交接。V1／V2 保留原有換手策略，方便比較。
- WiFi 的三條線各有自己的判定區。V3 可以一手張開覆蓋三條（預設手掌半徑 0.5 可以），或一手覆蓋中央與一側、另一手負責另一側（標示為 `L+R滑`）；手掌半徑太小時只能雙手分工。V1／V2 固定雙手分工。
- Slide 滑行途中可用設定的手掌半徑順帶完成附近 Touch，手仍沿原軌跡移動；不適用需要持續按住的 Touch Hold。
- WiFi 與其他形狀共用一個時間括號的複合寫法目前會回報位置與原因；各段獨立時間的接續寫法仍可使用。

以上是 Demo 的圓形手掌與動作近似。

## 8. 真人手順標註

「標註」分頁用來記錄真人實際的打法，作為調整模型參數的參照，也方便多人協作。標註存在目前開啟的譜面紀錄裡，會自動儲存。

**逐顆標註**
- 在盤面或清單點選音符，按 `A` 標左手、`D` 標右手，標完自動跳到下一步（可關閉）。有起點的 Slide 分成兩步，依時間和其他音符排在一起：先在起點被打到的時間標「起點」，到開始滑行的時間再標「滑行」；編輯區以淺色底標出目前這一步，也可以點「起點」「滑行」切換。想整顆用同一隻手，按 `Shift`+`A`／`D` 一次標完。WiFi 另可選「L+R」表示兩手分工。
- 換手：把播放時間拖到換手的那一刻，按「在 … 換手」；可以有多次換手。
- 信心：`S` 在「確定／不確定／皆可」之間切換。只有「確定」會拿來比對與限制求解；「皆可」表示兩隻手都常見。
- 每顆可以寫備註；`Delete` 清除這顆，`N` 跳到下一步還沒確認的。
- 「用模型預填空白」會先把模型的手順填成草稿（清單上以虛線框標示「預填」），逐顆確認（`Enter`）或修改後才算真人資料（Slide 只改其中一步時，另一步的預填會清掉，留給那一步自己標），可以省去從頭填的時間。清單可篩選還沒確認、預填、與模型不同、有備註或不確定的音符。

**備註**：填入你的名字，會記在每一顆標註上；另有整體備註與區段備註（可取循環範圍）。下方「一次清除」可以清除所有手順標註（保留備註）、所有備註（保留手順）或全部，按兩次才會執行，清除後可在提示上按「復原」。

**分享**：「複製標註」把標註連同原譜轉成標註檔文字（格式 `maimotion-hand-annotation`，每顆音符一行），可以直接貼給其他人，也可以下載成檔案。「匯入並合併」可貼上文字或開啟檔案，也可以直接在「新增」貼上標註檔（按鈕會變成「新增並匯入標註」）：以原譜比對，已有同一份譜面的紀錄就合併，沒有就連同原譜新增一筆紀錄；同一顆手順不同時，可選擇保留自己的或改用匯入的，備註兩邊都保留。原譜被改過（雜湊不符）的檔案會被拒絕，避免對錯音符。

**比對**（僅桌面版）：用「參數」分頁目前的設定，分別求出模型最佳解與「照你的標註打」的最佳解，顯示吻合率、兩者分數與差距，以及與模型不同的音符（點一下跳到該處）。差距越小，代表目前參數越認同真人打法。若模型照標註走不下去，會標出第一個卡住的時刻與音符。按「在盤面看照標註的打法」可以直接播放照標註求出的動作。

**影片同步**（搜尋與下載僅桌面版）：按分頁上方的「影片同步」會開一個獨立視窗，可以放在另一個螢幕，對照真人手元影片標註。
- 找影片：預設用「曲名＋難度＋maimai 手元」搜尋 YouTube，也可以改關鍵字或直接貼上影片網址。第一次使用要先下載 yt-dlp（約 18 MB）。YouTube 還需要 JS 執行環境：已安裝 Node.js 或 Deno 就直接使用，都沒有的話按「下載 Deno」（約 45 MB，免安裝，不需要系統管理員權限）；沒下載就去抓影片而失敗時，錯誤訊息也會附上這顆按鈕。影片只下載 720p 以下的畫面（沒有聲音），存在本機，不會上傳。也可以開啟自己錄的影片。
- 對齊：把影片播放或逐格（`←`／`→`）停在第一顆音符被打到的那一格，按「設為第一顆」。之後可用「±1 格」「±10ms」微調，再按「最後一顆」確認整首沒有跑掉。也可以對到目前選取的音符。
- 選音符時：在主視窗選到音符（包括標完自動跳到下一顆），影片預設會從判定前 0.4 秒開始以 0.5 倍速播放，播到判定後 0.3 秒，再停回判定那一格；也可以改成「只跳過去」或「不跟隨」。`R` 重播這顆。
- 兩邊會互相跟隨：已對齊時，播放／暫停與播放速度兩邊共用，在哪個視窗按都一樣；在影片視窗逐格或拖曳，主視窗盤面跟著動（快速拖曳或按住方向鍵時，畫面會盡快跟上最後的位置）；在主視窗拖曳時間軸，影片跟著動。影片還沒對齊或停在譜面開始之前（片頭）時，影片視窗的播放只播影片。一起播放時影片以微調速度對齊主視窗，不會中途跳格；2× 時螢幕一秒顯示不了影片的全部畫面，要逐格看快速的手部動作請用 0.5× 或逐格。
- 在影片視窗也能直接標註：`A`／`D`／`S`／`Enter`／`N`／`Delete` 會送到主視窗，`↑`／`↓` 切換上一顆／下一顆音符，`Space` 播放影片。
- 影片與對齊偏移存在這份譜面的標註裡；另外會照「譜面＋影片」記住對齊，換過影片、解除連結或刪掉紀錄重新加入後，選回同一部影片會自動帶回上次的對齊。分享標註檔時會附上 YouTube 網址與對齊偏移，對方匯入後一鍵下載就能直接對齊；本機影片不會分享出去。
- 設定裡的「影片同步」可以更新 yt-dlp、刪除個別影片或一次清除所有下載的影片；刪除後標註裡的影片連結與對齊仍會保留，需要時再下載即可。

音符以「時間｜種類｜位置」的穩定鍵對應（例如 `12.345|tap|3`、`20.000|slide|1-5`），不依賴會隨版本變動的音符編號。

